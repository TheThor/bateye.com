# You can find more information about this file on the symfony website:
# http://www.symfony-project.org/reference/1_4/en/08-Security

sf_guard_user:
  credentials: super_users

autoSfGuardGroup:
  credentials: super_users

sf_guard_permission:
  credentials: super_users

sf_guard_register:
  credentials: super_users

sf_guard_user:
  credentials: [[super_users]]

all:
  is_secure: true
  credentials: [[super_users, admins]]

