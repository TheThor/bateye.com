generator:
  class: sfDoctrineGenerator
  param:
    model_class:           Lovers
    theme:                 bootstrap
    non_verbose_templates: true
    with_show:             false
    singular:              ~
    plural:                ~
    route_prefix:          lovers
    with_doctrine_route:   true
    actions_base_class:    sfActions

    config:
      actions: ~
      fields:
        display: [name]
      list:
        display: [name, designer, collection_id, created_at, updated_at]
      filter:
        display: [name, description, citation, designer, collection_id, created_at, updated_at]
      form: ~
#        display: [collection_id, category_id, main_img_path, main_img_alt, name, description, citation, designer, width, height, diameter, depth, materials, other]
      edit:    ~
      new:     ~
